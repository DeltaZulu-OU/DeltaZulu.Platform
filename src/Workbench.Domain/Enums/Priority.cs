namespace Workbench.Domain.Enums;

/// <summary>Issue priority levels. POC uses a simple four-level scale.</summary>
public enum Priority
{
    Low = 0,
    Normal = 1,
    High = 2,
    Critical = 3,
}